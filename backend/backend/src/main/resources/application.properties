spring.application.name=backend
# PORT
server.port=8080

# PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/zakatnow
spring.datasource.username=alwan
spring.datasource.password=

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# EMAIL
spring.mail.host=${SMTP_HOST}
spring.mail.port=${SMTP_PORT}
spring.mail.username=${SMTP_USERNAME}
spring.mail.password=${SMTP_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.default-encoding=UTF-8
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000


# JWT Secret
# app.jwt.secret=secretZakatNow25
# app.jwt.expirationMs=86400000

# Xendit Payment Gateway
xendit.api.key=xnd_development_pXzsCUuT5ulWCO3S6PQWWdp3SUwZn9gTRtqRQEaO0ZJAk31yfFT1eNPwFMzq4Db1
